1、主文件在src下面，主文件为selectword.py
2、记得更改语料的路径，在selectword.py里面，即主函数的参数doc。
   注意，参数doc是读取出来是一行一行。
    doc = oepn("path",'r').read()    # path即为你的语料
3、然后运行该主文件即可。文件的结果是一个个词，这些词包括结巴词库的词，也有分出来的新词
4、将（3）中的词进行过滤（用jieba的词典），即可得到新词。